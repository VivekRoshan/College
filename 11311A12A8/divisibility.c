/*programme to find divisibility of two numbers*/
#include<stdio.h>
main()
{
int a,b;
printf("enter a,b values");
scanf("%d%d",&a,&b);
if(a%b==0)
printf("a is divisible by b");
else
printf("a is not divisible by b");
return 0;
}

